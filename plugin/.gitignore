lib/
bin/
obj/
# Native SimConnect.dll copied here at build time from the CTrue.FsConnect NuGet package
# for Costura.Fody to embed -- not ours to redistribute via source control, same reasoning
# as lib/ above; the copy target regenerates it from the package cache on every build.
costurax64/
# Auto-regenerated by Fody on every build (IDE IntelliSense schema for FodyWeavers.xml).
FodyWeavers.xsd
